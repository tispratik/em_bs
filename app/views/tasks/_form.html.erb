

 <!-- <p>
   <label for="project_name">Project</label>
 
  <%= f.select :id, Project.all.collect {|c| [c.name,  c.id] }, { :include_blank => '--Select--' }  %>
  </p> -->



<p>
  <label for="task_name">Name:</label>
  <%= f.text_field :name %>
</p>


<% @users = User.find(:all) %>

 <p>
  <label for="task_description">Assign To:</label>
   <%= f.select :assign_to, @users.collect { |c| [c.username, c.id ] }, { :include_blank => '--Select--' }%>
 </p>
