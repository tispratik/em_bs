<h1>Editing Asset</h1>

<%= error_messages_for :asset %>

<% form_for(:asset, :url => [@project, @asset], :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Update"%>
  </p>
<% end %>

<br/>

<%= link_to 'Show', [@project, @asset] %>
|
<%= link_to 'Back', project_assets_path %>