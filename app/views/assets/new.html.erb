<h1>New Asset</h1>

<%= error_messages_for :asset %>

<% form_for(:asset, :url => project_assets_path) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>
<br/>
<%= link_to 'Back', project_assets_path %>